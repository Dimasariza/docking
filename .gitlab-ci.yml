image: node:16.13.0
pages:
  cache:
    paths:
      - node_modules/
  script:
    - npm install -g @angular/cli
    - npm i
    - npm run build
  artifacts:
    paths:
      - dist
  only:
    - main